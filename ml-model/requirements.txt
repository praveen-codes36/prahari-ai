# RoadGuard AI: Machine Learning Subsystem & Web Dashboard Dependencies
# Python 3.10+ / 3.11+ Compatible
#
# NOTE: torch and torchvision are intentionally NOT listed here.
# Dockerfile installs them separately from the CPU-only wheel index
# (https://download.pytorch.org/whl/cpu) to avoid pulling the ~2GB
# CUDA build that free hosts don't need and often can't fit.
# If installing locally for development, run:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

xgboost>=2.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
Pillow>=9.5.0
joblib>=1.3.0
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
matplotlib>=3.7.0
seaborn>=0.12.0
pytest>=7.3.0
requests>=2.31.0
transformers>=4.30.0
networkx>=3.0
